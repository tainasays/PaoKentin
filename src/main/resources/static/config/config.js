// Configurações globais da aplicação
const CONFIG = {
    API_BASE: 'http://localhost:8080/paokentin',
    ENDPOINTS: {
        PAES: '/paes',
        PAES_BY_ID: '/paes/{id}'
    }
};


window.CONFIG = CONFIG;
